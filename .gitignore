# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# Dependências
/node_modules

# Arquivos de Build
/dist
/build

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite
.vite/

# Variáveis de ambiente (os *.example ficam versionados)
.env
.env.*
!.env.example
# O `.env.*` acima engoliria também o exemplo do build de produção, que É para
# ser versionado — é ele que documenta a única variável sem a qual o build
# falha (Fase F-0). Mesmo padrão do `.env.test.example` no backend.
!.env.production.example
